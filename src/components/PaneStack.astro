<div id="pane-stack"></div>
<style>
  /* Keep the stack off the header (adjust 64px to your Header height) */
  #pane-stack{
    position:fixed; top:64px; right:0; height:calc(100vh - 64px);
    width:min(1200px,60vw); display:flex; gap:8px; padding:12px;
    overflow-x:auto; z-index:95; pointer-events:none; visibility:hidden;
  }
  #pane-stack.active{ pointer-events:auto; visibility:visible; }
  .pane{
    pointer-events:auto; background:var(--sl-color-bg,#fff);
    border:1px solid rgba(0,0,0,.1); border-radius:12px;
    box-shadow:0 10px 40px rgba(0,0,0,.2);
    width:420px; height:calc(100vh - 64px - 24px);
    overflow:auto; overscroll-behavior:contain;
  }
  .pane header{ display:flex; justify-content:space-between; align-items:center;
    padding:.6rem .8rem; border-bottom:1px solid rgba(0,0,0,.06) }
  .pane .close{ border:1px solid rgba(0,0,0,.12); border-radius:.45rem; padding:.15rem .45rem }
  @media (max-width: 1024px){ #pane-stack{ display:none } }
</style>
