<div id="pane-stack"></div>
<style>
  /* Andy-style sliding panes - side by side, no overlap */
  #pane-stack{
    position:fixed; 
    top:64px; 
    right:0; 
    bottom:0;
    height:calc(100vh - 64px);
    display:flex;
    flex-direction:row;
    align-items:stretch;
    gap:0;
    padding:0;
    overflow-x:auto; 
    overflow-y:hidden;
    z-index:var(--z-panes); 
    pointer-events:none; 
    visibility:hidden;
    transition:visibility 0.2s;
    /* Hide scrollbar but keep functionality */
    scrollbar-width:none;
  }
  #pane-stack::-webkit-scrollbar{
    display:none;
  }
  #pane-stack.active{ 
    pointer-events:auto; 
    visibility:visible; 
  }
  
  .pane{
    pointer-events:auto; 
    background:var(--c-bg);
    border-left:1px solid var(--c-border); 
    box-shadow:-4px 0 20px rgba(0,0,0,0.15);
    min-width:500px;
    max-width:500px;
    width:500px;
    height:100%;
    overflow-y:auto;
    overflow-x:hidden;
    overscroll-behavior:contain;
    flex-shrink:0;
    flex-grow:0;
    isolation:isolate;
    position:relative;
  }
  
  .pane header{ 
    display:flex; 
    justify-content:space-between; 
    align-items:center;
    padding:1.25rem; 
    border-bottom:1px solid var(--c-border);
    background:var(--c-bg-soft);
    position:sticky; 
    top:0;
    z-index:10;
    margin-top:0;
  }
  
  .pane header strong{
    font-size:1.1rem;
    font-weight:600;
    color:var(--c-text);
  }
  
  .pane .close{ 
    background:transparent; 
    border:1px solid var(--c-border-focus); 
    border-radius:var(--c-radius-sm); 
    padding:.35rem .7rem; 
    cursor:pointer;
    font-size:1.3rem; 
    line-height:1;
    color:var(--c-text-muted);
    transition:all 0.15s ease;
  }
  
  .pane .close:hover{ 
    background:var(--c-bg-muted); 
    color:var(--c-text);
  }
  
  .pane .body{ 
    padding:2rem 1.5rem; 
  }
  
  @media (max-width: 1024px){ 
    #pane-stack{ display:none } 
  }
</style>
